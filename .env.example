# Copiar a .env y completar con valores reales
# NUNCA subir .env a git

TELEGRAM_BOT_TOKEN=7xxxxxxxxxx:AAxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TELEGRAM_CHAT_ID=           # ID del chat del técnico (obtener con @userinfobot)
# 1 = despliegue cliente (Bogotá): solo herramientas de campo; la IA usa /app/docs/campo/*.md (incluidos en imagen).
#    No monta CLAUDE.md al contenedor — el técnico no recibe arquitectura interna por el modelo.
# 0 = laboratorio USB: modo developer opcional; podés montar CLAUDE/SISTEMA en /app/docs/developer/ (compose comentado).
AGENT_TECHNICIAN_ONLY=1
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── Proveedor LLM para conversación interactiva del técnico ────────────────
# groq   = solo Groq (default, gratis)
# openai = GPT-4o mini para chat + Groq para monitores background
# Si OpenAI falla o supera caps → fallback automático a Groq.
LLM_PROVIDER_INTERACTIVE=groq

# OpenAI — https://platform.openai.com/api-keys
# Límite mensual en web: Settings → Limits → Monthly budget (ej. $5)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# Si el lab tiene dos rutas (cable + WiFi), IP local WiFi para salir a OpenAI
# Ejemplo lab .205: 10.0.0.109 — dejar vacío si una sola ruta a internet
OPENAI_BIND_IP=

# Hard caps OpenAI en el servidor — techo ~$0.10/mes por Shomer (además del límite $5 web)
OPENAI_LIMIT_PER_MESSAGE=2000
OPENAI_LIMIT_PER_USER_DAILY=8000
OPENAI_LIMIT_DAILY=12000

# Umbrales globales (todos los proveedores) — activan modo mantenimiento
TOKEN_WARN_DAILY=80000
TOKEN_LIMIT_DAILY=120000

# URL del Shomer local (no cambiar en producción)
SHOMER_URL=http://127.0.0.1:8000
SHOMER_USER=admin
SHOMER_PASS=

# Archivo de dispositivos
DEVICES_FILE=/app/data/devices.json

# ── Escalamiento de incidentes recurrentes (equipos flapping) ──────────────
# En vez de un mensaje Telegram por cada caída/recuperación de un equipo que
# sigue fallando, agrupa en una ventana y pide confirmación al técnico antes
# de escalar. Ver core/incident_escalation.py.
ESCALATION_ENABLED=1
ESCALATION_AGG_WINDOW_SEC=3600         # ventana de agregación silenciosa (1h)
# Plazos largos a propósito: el técnico de campo no vive en el sitio, solo
# visita periódicamente — 30 min sería irreal, terminaría escalando siempre.
ESCALATION_ACK_TIMEOUT_SEC=43200       # espera tras el resumen antes de recordar (12h)
ESCALATION_REMINDER_TIMEOUT_SEC=43200  # espera tras el recordatorio antes de escalar (12h)
ESCALATION_GRACE_HOURS=4               # botón "lo resuelvo ahora" (remoto/mismo día)
ESCALATION_GRACE_VISIT_HOURS=168       # botón "próxima visita" (problema físico, 7 días)
ESCALATION_CLOSE_AFTER_HOURS=6         # cierre automático sin fallas nuevas (no aplica
                                        # mientras un incidente esté en gracia "próxima visita")
# Chat Telegram del coordinador de soporte para escalamientos. Si se deja
# vacío, usa AGENT_DEVELOPER_CHAT_ID como respaldo.
SUPPORT_COORDINATOR_CHAT_ID=

# Correo opcional en escalamiento final (si no se configura, se omite el paso
# de correo sin error — el aviso por Telegram al coordinador igual se manda).
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SUPPORT_EMAIL_FROM=
SUPPORT_EMAIL_TO=
