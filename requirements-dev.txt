# Dependencias solo para desarrollo/pruebas -- NO se instalan en la imagen
# Docker de producción (el Dockerfile solo usa requirements.txt).
#
# Uso:
#   python3 -m venv .venv-test && source .venv-test/bin/activate
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest
pytest>=8.0
